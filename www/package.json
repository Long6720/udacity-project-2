{
  "name": "udacity-project-2",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf www/ && mkdir www",
    "build": "npm run clean && cp package.json www/package.json && cd www && zip -r Archive.zip . && cd ..",
    "dev": "node src/server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Long6720/udacity-project-2.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "jimp": "^0.16.2"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
